<!--pages/gift/giftList.wxml-->

<i-cell-group>
    <i-cell is-link url="/pages/gift/fragments?giftId={{item._id}}&imgSrc={{item.imgSrc}}"  wx:for="{{marketList}}" wx:key="index">
    <view class="li">
      <image src="{{item.imgSrc}}"></image>
      <view class="title">{{item.giftName}}</view>
      </view>
    </i-cell>
</i-cell-group>